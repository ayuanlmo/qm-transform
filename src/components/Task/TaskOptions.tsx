import * as React from "react";
import {Divider} from "@fluentui/react-components";

const TaskOptions: React.FC = (): React.JSX.Element => {
    return (
        <div>
            <Divider/>
        </div>
    );
};

export default TaskOptions;
